using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class PortraitContainer : MonoBehaviour
{
    //[SerializeField] GameObject portraitPrefab;
    //[SerializeField] GameObject starterPokemon;
    //private void Start()
    //{
    //    AddPortrait(starterPokemon.GetComponent<PokemonContainer>());
    //    AddPortrait(starterPokemon.GetComponent<PokemonContainer>());
    //    AddPortrait(starterPokemon.GetComponent<PokemonContainer>());
    //}

    //void AddPortrait(PokemonContainer pokemon)
    //{
    //    portraitPrefab.GetComponent<Portrait>().pokemonContainer = pokemon;
    //    portraitPrefab.GetComponent<Image>().sprite = pokemon.pokemon.sprite;

    //    Instantiate(portraitPrefab, new Vector3(0, 0, 0), Quaternion.identity, transform);

    //}
}
